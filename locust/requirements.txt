locust==2.42.1
prometheus_client==0.21.0
urllib3
boto3==1.40.64
botocore==1.40.64
rich==13.7.1
# Spark Connect client (used when load testing Spark Connect endpoints).
# Match the EMR Spark runtime (EMR 7.x = Spark 3.5.x). Pulls in grpcio,
# pandas, pyarrow, numpy, protobuf.
pyspark[connect]==3.5.5